# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Feature 12: Shape Panel

## Current Goal

- Add the next planned feature unit here.

## Completed

- Feature 01: Design System (`context/feature-specs/01-design-system.md`) — shadcn/ui installed and configured (`components.json`, `lib/utils.ts`); Button, Card, Dialog, Input, Tabs, Textarea, and ScrollArea added under `components/ui/`; `lucide-react` installed; dark-only theme tokens from `context/ui-context.md` wired into `app/globals.css` via `@theme inline`, mapped onto shadcn's semantic tokens (`background`, `foreground`, `card`, `primary`, etc.) so the unmodified `components/ui/*` files render correctly; `dark` class forced on `<html>` in `app/layout.tsx` since there is no light mode. Verified via `tsc --noEmit` and `next build` (temporary route exercising all 7 components + `cn()`, then removed).
- Feature 02: Editor Chrome (`context/feature-specs/02-editor-chrome.md`) — `components/editor/editor-navbar.tsx`: `h-14` navbar with left/center/right sections, left section holds a sidebar toggle `Button` swapping `PanelLeftOpen`/`PanelLeftClose` based on an `isSidebarOpen` prop, dark `bg-surface` with `border-b border-surface-border`, controlled via `isSidebarOpen`/`onToggleSidebar` props (state owned by the parent that composes it). `components/editor/project-sidebar.tsx`: `fixed` overlay positioned below the navbar (`top-14`) so it floats above the canvas without affecting layout flow, slides in via `-translate-x-full` → `translate-x-0` on `isOpen`, header with "Projects" title + close button, shadcn `Tabs` (My Projects / Shared) both with empty placeholder text, full-width `New Project` button with `Plus` icon in the footer; accepts `isOpen`/`onClose` props. `components/editor/editor-dialog.tsx`: reusable dialog pattern — `EditorDialogContent` and `EditorDialogFooter` wrap the unmodified `components/ui/dialog.tsx` primitives with brand tokens (`rounded-3xl`, `bg-surface`, `border-surface-border`) per the `ui-context.md` modal spec, re-exporting `Dialog`/`DialogTrigger`/`DialogClose`/`DialogHeader`/`DialogTitle`/`DialogDescription` unchanged; no concrete dialog instances were built, per spec. `components/editor/editor-shell.tsx`: client component composing `EditorNavbar` + `ProjectSidebar` around a `children` slot for the future canvas, owning the `isSidebarOpen` state itself (`useState`). Mounted at the root route (`app/page.tsx` now renders `EditorShell` with a placeholder "canvas area" `children`), replacing the default create-next-app boilerplate. Verified via `tsc --noEmit`, `eslint`, and `next build`, plus Playwright-driven checks against both temporary preview routes and the live root route (toggle behavior, canvas position unaffected, tab switching, dialog open state, sidebar open/close, screenshots) — temporary routes removed after verification.
- Feature 03: Auth (`context/feature-specs/03-auth.md`) — `@clerk/ui` installed (theme constants only). `proxy.ts` at the project root (Next.js 16 renamed `middleware.ts` → `proxy.ts`; same runtime contract) wraps `clerkMiddleware` from `@clerk/nextjs/server`, builds an `isPublicRoute` matcher from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL` (added to `.env.local`, standard Clerk var names — they didn't exist yet), and calls `auth.protect()` for every non-public route; matcher config excludes static assets and always runs on `/api`. `app/layout.tsx` wraps `<html>` in `ClerkProvider` with `appearance={{ theme: dark, variables: {...} }}` — `dark` imported from `@clerk/ui/themes`, and every color/font variable (`colorBackground`, `colorForeground`, `colorPrimary`, `colorDanger`, `colorBorder`, `fontFamily`, etc.) is mapped to the app's existing `--bg-*`/`--text-*`/`--accent-*`/`--state-*`/`--font-geist-*` CSS custom properties via `var(...)`, no hardcoded colors. `components/auth/auth-layout.tsx`: shared two-panel shell for auth pages — left panel (`lucide-react` `Ghost` icon + "Ghost AI" wordmark, one-line tagline, three-item text-only feature list) hidden below `lg`, right panel centers the `children` slot; no gradients, hero sections, cards, or scroll. `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` render Clerk's `<SignIn />`/`<SignUp />` inside `AuthLayout`. `app/editor/page.tsx` now holds the placeholder canvas page (moved out of `app/page.tsx`, resolving the prior open question below); `app/page.tsx` is now a server component that calls `auth()` and redirects to `/editor` when signed in, `/sign-in` otherwise (the unauthenticated branch is a defensive fallback — `proxy.ts` already redirects unauthenticated requests to `/` before the page body runs, since `/` isn't a public route). `components/editor/editor-navbar.tsx`: added Clerk's `UserButton` to the existing empty right-section `div` — no layout changes needed, it inherits the global `ClerkProvider` appearance. Verified via `tsc --noEmit`, `eslint`, `next build`, and a Playwright-driven check of the running dev server (`/` → `/sign-in` redirect, `/editor` → `/sign-in` redirect when signed out, sign-in/sign-up screenshots at 1440×900 and 375×800 confirming the two-panel/form-only split and token-driven dark styling, `console --errors` clean).
- Feature 04: Project Dialogs & Editor Home (`context/feature-specs/04-project-dialogs.md`) — `types/project.ts`: `Project` interface (`id`, `name`, `slug`, `role: "owner" | "collaborator"`). `hooks/use-project-dialogs.ts`: the dedicated hook the spec asked for — owns an in-memory mock `projects` array (seeded with 2 owned + 1 collaborator project), dialog state (`{ type: "create" | "rename" | "delete", project? }` discriminated union), form state (`name`, derived `slug` via a local `slugify()`), and `isLoading`; exposes `openCreateDialog`/`openRenameDialog(project)`/`openDeleteDialog(project)`/`closeDialog` plus async `createProject`/`renameProject`/`deleteProject` that simulate a 400ms delay and mutate the mock array directly (no API calls, per spec). `components/editor/project-dialogs.tsx`: presentational `ProjectDialogs` component consuming the hook's return value, rendering all three dialogs (Create/Rename/Delete) as controlled `Dialog open={dialog?.type === "..."}` instances built on `editor-dialog.tsx`'s wrappers — Create has a name input plus a live slug preview paragraph below it; Rename prefills the input from `dialog.project.name`, shows `Renaming "<name>"` in the description, autofocuses + selects the input via a `useEffect`+`ref` keyed on `dialog`, and submits on Enter; Delete has no input, just a description and a `variant="destructive"` confirm button. `components/editor/editor-home.tsx`: the `/editor` empty-state (`EditorHome`) — heading, description, and a `New Project` button with a `Plus` icon, no cards, per spec. `components/editor/project-sidebar.tsx`: now takes `myProjects`/`sharedProjects`/`onNewProject`/`onRenameProject`/`onDeleteProject` props instead of owning static placeholder tabs; each owned-project row shows `Pencil`/`Trash2` icon buttons (revealed on row hover/focus) wired to the rename/delete openers, shared-project rows render name-only with no actions; added a `lg:hidden` backdrop scrim (`fixed inset-0 top-14`, click → `onClose`) behind the sidebar for the mobile tap-outside-to-close behavior — desktop is unaffected since the scrim only exists below the `lg` breakpoint. `components/editor/editor-shell.tsx`: now owns `useProjectDialogs()` as the single source of truth, passing project data + openers down to `ProjectSidebar`, rendering `EditorHome` in the canvas slot (replacing the placeholder), and mounting `ProjectDialogs` once at the shell level — dropped the generic `children: ReactNode` prop since there's only one thing rendered in that slot right now (see Architecture Decisions). `app/editor/page.tsx` simplified to `return <EditorShell />` accordingly. Verified via `tsc --noEmit`, `eslint .` (both clean, whole repo), `next build`, and a Playwright-driven check of the running dev server — since `/editor` sits behind Clerk auth and no test session was available, verification used a temporary public route (`app/preview-editor/page.tsx` + a temporary `isPublicRoute` entry in `proxy.ts`, both reverted after) rendering the same `EditorShell`: confirmed the home heading/description/button, sidebar My Projects/Shared tabs with seeded mock rows, rename dialog (prefilled + description + focus ring visible), delete dialog (destructive red confirm button), create dialog's slug preview updating live while typing (`"My New System Design!"` → `my-new-system-design`), and the mobile (375×800) backdrop scrim closing the sidebar on an outside tap — `console --errors` clean throughout, screenshots taken at 1440×900 and 375×800.
- Prisma schema scaffold (`context/architecture-context.md`'s "Database" row) — ran `prisma init --datasource-provider postgresql --output ../app/generated/prisma`, producing `prisma/schema.prisma` (`generator client` with `output = "../app/generated/prisma"`, `datasource db { provider = "postgresql" }`), `prisma7.config.ts` (reads `DATABASE_URL` from env via `dotenv/config`), and `.env` (`DATABASE_URL` placeholder, already covered by the existing `.env*` gitignore rule). `/app/generated/prisma` added to `.gitignore`. No models defined yet — schema is empty pending the actual `projects`/`collaborators`/`specs`/`task runs` models. `DATABASE_URL` still needs to be set and `prisma migrate dev` run before the client is usable.
- Feature 05: Prisma Schema And Data Layer (`context/feature-specs/05-prisma.md`) — `prisma/models/project.prisma` (multi-file schema; `prisma7.config.ts`'s `schema: "prisma/"` already points at the directory, so Prisma merges it with `prisma/schema.prisma`'s `generator`/`datasource` blocks automatically — no preview flag needed on this Prisma version): `Project` (`id` cuid, `ownerId` — Clerk user id, `name`, optional `description`, `status ProjectStatus @default(DRAFT)` enum `DRAFT`/`ARCHIVED`, optional `canvasJsonPath`, `createdAt`/`updatedAt`, `@@index([ownerId])`, `@@index([createdAt])`) and `ProjectCollaborator` (`id` cuid, `projectId` + `project` relation with `onDelete: Cascade`, `email`, `createdAt`, `@@unique([projectId, email])`, `@@index([email])`, `@@index([projectId, createdAt])`). Only the fields the spec listed were added — no `slug` field, even though `types/project.ts`'s mock `Project` type has one; reconciling the mock UI type with the real schema is deferred to whichever future feature wires `app/api` routes to Prisma. `lib/prisma.ts`: cached singleton branching on `DATABASE_URL`'s scheme — `prisma+postgres://` → `new PrismaClient({ accelerateUrl: databaseUrl })` (Prisma 7's native `accelerateUrl` constructor option, no `@prisma/extension-accelerate` install needed since the spec's dependency list didn't include it and the option works standalone); anything else → `@prisma/adapter-pg`'s `PrismaPg` adapter. Cached on `globalThis` in non-production. Ran `prisma migrate dev --name add_project_models` against the `DATABASE_URL` already in `.env` (a Prisma Postgres pooled `postgres://...@pooled.db.prisma.io` connection string — direct-adapter path, not the Accelerate branch) — applied cleanly, `prisma/migrations/20260826121905_add_project_models/migration.sql` created (enum, both tables, both index sets, unique constraint, cascade FK, all matching spec). Ran `prisma generate` — output at `app/generated/prisma/` has no `index.ts`/`package.json` in this Prisma 7 output shape, only `client.ts` (plus `enums.ts`, `models.ts`, `internal/`, `browser.ts`); `lib/prisma.ts` imports from `@/app/generated/prisma/client` specifically, not the bare `@/app/generated/prisma` directory path. Verified via `prisma validate`, `tsc --noEmit`, `eslint .`, and `next build` — all clean.
- Feature 06: Project APIs (`context/feature-specs/06-project-apis.md`) — `app/api/projects/route.ts`: `GET` lists `prisma.project.findMany({ where: { ownerId: userId }, orderBy: { createdAt: "desc" } })` (current user's *owned* projects — the spec only says "list current user's projects" and never mentions collaborators for this feature, so collaborator-inclusive listing was treated as out of scope rather than inferred; see Open Questions), `POST` creates via `prisma.project.create({ data: { ownerId: userId, name } })`, defaulting `name` to `"Untitled Project"` when the parsed body's `name` is missing/blank; no manual `id` is set, so the schema's `@default(cuid())` strategy is used untouched. `app/api/projects/[projectId]/route.ts`: `PATCH` and `DELETE` both `findUnique` the project first (404 if missing), compare `project.ownerId !== userId` (403 if not the owner), then mutate; `PATCH` additionally 400s if the parsed `name` is missing/blank, `DELETE` returns `204` with an empty body. Both route files follow the `isAuthenticated`/`userId` destructure from `await auth()` and the `401` JSON pattern recommended by the `clerk-nextjs-patterns` skill's `references/api-routes.md` (not the older `!!userId` style `app/page.tsx` used in Feature 03) — this is an explicit handler-level check, kept regardless of `proxy.ts`'s middleware-level `auth.protect()`, since that's the documented pattern specifically because middleware behavior isn't guaranteed to produce the right status/shape for API callers. Request bodies are parsed as `unknown` and narrowed through a small `parse*Input` type guard per file (no `any`), per the "validate unknown external input at system boundaries" standard. No UI wiring, per spec. Verified via `tsc --noEmit`, `eslint app/api` (clean), and `next build` (`/api/projects` and `/api/projects/[projectId]` both listed as dynamic `ƒ` routes, build succeeds). The `401`/`403`/`404` branching itself was verified by code review against the Clerk-recommended snippet (see Session Notes for why live `curl` testing of the `401` path was inconclusive in this environment); the Prisma-backed logic each branch depends on (default name, cuid `id`, ownerId scoping, rename, delete, not-found lookups) was verified directly against the real `DATABASE_URL` with a throwaway `tsx` script exercising `prisma.project.create/findMany/update/findUnique/delete` the same way the route handlers do, then deleting the test row — all matched expected behavior.

- Feature 07: Wire Editor Home (`context/feature-specs/07-wire-editor-home.md`) — `lib/projects.ts`: new shared data helper, `getOwnedProjects(ownerId)` (the query `app/api/projects/route.ts`'s `GET` used inline in Feature 06, now extracted and reused by both the route and the server components below) and `getSharedProjects(email)` (`prisma.project.findMany({ where: { collaborators: { some: { email } } } } })`, returns `[]` for an empty email rather than querying). `lib/auth.ts`: `getCurrentUserEmail()` wraps `currentUser()` from `@clerk/nextjs/server` (`user?.primaryEmailAddress?.emailAddress ?? ""`) — needed because `ProjectCollaborator` (Feature 05) keys collaborators by `email`, not Clerk user ID, and `auth()` alone doesn't expose it. `types/project.ts`: the mock `Project` type's `slug`/`role` fields are gone — now just `{ id, name }`, resolving the open question from Feature 04/06 (dropped `slug` rather than adding it to the schema, since "room ID" turned out to be a create-time-only concept, not a persisted field; dropped `role` since the spec fetches owned/shared as two separate lists rather than one list with a role tag, matching how `ProjectSidebar` already consumed them). `app/editor/page.tsx`: now an async server component — `auth()` for `userId` (redirects to `/sign-in` if missing, defensive fallback since `proxy.ts` already blocks this), fetches `getOwnedProjects(userId)` and `getSharedProjects(email)` in parallel via `Promise.all`, passes both lists as props into `EditorShell`. No client-side fetching for the initial load, per spec. `app/editor/[projectId]/page.tsx`: new route (didn't exist before this feature) — same data-fetching shape as `/editor` plus `prisma.project.findUnique({ where: { id: projectId }, include: { collaborators: true } })` for the active project; `notFound()` if the project doesn't exist *or* the current user is neither the owner nor a collaborator by email (folded into one `notFound()` rather than a `403`, so a non-member can't distinguish "doesn't exist" from "not yours" — this route wasn't specified by any feature-spec yet, but "navigate to the new workspace" and "redirect to `/editor` if deleting the active workspace" in `07-wire-editor-home.md` both presuppose a per-project workspace URL exists to navigate to/away from, so a minimal one was added; see Open Questions). `hooks/use-project-actions.ts` (replaces `hooks/use-project-dialogs.ts` — deleted): same dialog-state shape as the old hook (`{ type: "create" | "rename" | "delete", project? }`), but mutations now hit the real API instead of mutating an in-memory array — `createProject` `POST`s `{ name }` to `/api/projects`, then `router.push(`/editor/${project.id}`)` using the real returned `id`; `renameProject` `PATCH`s `/api/projects/[id]`, then `router.refresh()`; `deleteProject` `DELETE`s `/api/projects/[id]`, then either `router.push("/editor")` (if `project.id === activeProjectId`, passed in via the hook's options) or `router.refresh()`. No local project-array state remains in the hook — `router.refresh()` re-runs the server component's data fetch, which is the actual source of truth for both `/editor` and `/editor/[projectId]`. Also generates a client-only "room ID preview" (`slugify(name) + "-" + <5-char base36 suffix>`, suffix regenerated each time the create dialog opens) shown live in the create dialog — this is a *preview only*; it is never sent to the API. `components/editor/project-dialogs.tsx`: `slug` → `roomId`, preview text now reads `Room ID: <roomId>`; prop renamed `dialogs` → `actions` to match the new hook name. `components/editor/editor-shell.tsx`: now takes `ownedProjects`/`sharedProjects`/`activeProject?` as props (server-fetched, passed down from the two page components) instead of owning `useProjectDialogs()` internally against mock data; renders `EditorWorkspace` (new, minimal placeholder — project name + "Canvas coming soon.") when `activeProject` is set, `EditorHome` otherwise. `app/api/projects/route.ts`: `GET` now calls `getOwnedProjects(userId)` instead of inlining the query — behavior unchanged, this is the refactor that made the "existing project data helper" the spec referenced actually exist. `POST`/`PATCH`/`DELETE` routes themselves are unchanged from Feature 06 (no client-supplied `id` support was added — see Architecture Decisions on why the room ID preview stays client-only). Verified via `tsc --noEmit`, `eslint .`, and `next build` (all clean; `/editor` and `/editor/[projectId]` both listed as dynamic `ƒ` routes). `lib/projects.ts`'s two functions were also verified directly against the real `DATABASE_URL` with a throwaway `tsx` script (same pattern as Feature 06): created one project owned by a fake `ownerId` and one project with a fake-email collaborator, confirmed `getOwnedProjects`/`getSharedProjects` each returned exactly the expected row and that `getSharedProjects("")` returns `[]` without querying, then deleted both test rows. End-to-end browser verification (create → navigate → rename → refresh → delete → redirect) was **not** done this session — no browser automation tool was available (see Session Notes); this is the same Clerk-session bottleneck Features 04 and 06 hit, now also blocking verification of `router.push`/`router.refresh` behavior specifically.

- Feature 08: Editor Workspace Shell (`context/feature-specs/08-editor-workspace-shell.md`) — `app/editor/[projectId]/page.tsx` renamed to `app/editor/[roomId]/page.tsx` (per the spec's `/editor/[roomId]` route naming; `roomId` and `project.id` are the same value, per Feature 07's room-ID-alignment decision — no schema change). `lib/project-access.ts`: `getCurrentIdentity()` (`{ userId, email }`, composing `auth()`'s `userId` with the existing `lib/auth.ts`'s `getCurrentUserEmail()` rather than duplicating the `currentUser()` call) and `hasProjectAccess(project, identity)` (owner-id match or collaborator-email match, `false` if `userId` is null) — the two "access helpers" the spec asked for, kept outside the page component. `components/editor/access-denied.tsx`: centered layout, `Lock` icon in a rounded `bg-elevated` tile, "Access denied" heading + short message, `Link` back to `/editor` — new, unstyled foundation component (not touching `components/ui/*`). `app/editor/[roomId]/page.tsx`: redirects to `/sign-in` if unauthenticated, otherwise fetches the project (with `collaborators`) plus both project lists in parallel; renders `<AccessDenied />` directly (no `notFound()`) when the project doesn't exist *or* `hasProjectAccess` is false — both cases now show the same `AccessDenied` UI per spec, replacing Feature 07's `notFound()` placeholder behavior. `components/editor/editor-navbar.tsx`: new optional props `projectName` (rendered centered, truncated), `isAiSidebarOpen`/`onToggleAiSidebar` (AI sidebar toggle, `Sparkles` icon, `text-ai-text` when open), `onShare` (`Share2` icon button, no handler wired — sharing behavior is out of scope for this feature per spec). Share/AI-toggle buttons only render when `onToggleAiSidebar` is passed, so the plain `/editor` home view (no active project) is unaffected. `components/editor/ai-sidebar.tsx`: new right-side placeholder panel, styled like `ProjectSidebar` (fixed overlay, `top-14`, slide-in via translate, dark blurred surface) but sliding in from the right; centered `Sparkles` icon + "AI chat coming soon." message — the "right sidebar placeholder for future AI chat" the spec asked for, no chat logic. `components/editor/project-sidebar.tsx`: new `activeProjectId` prop; both the My Projects and Shared list items get `bg-accent-dim`/`text-brand` styling plus `aria-current` when their `project.id` matches, satisfying "current room highlighted in the sidebar." `components/editor/editor-workspace.tsx`: added explicit `bg-base` to the canvas placeholder div (previously relied on inheriting it from the shell root) per the spec's "dark background" requirement — message content unchanged. `components/editor/editor-shell.tsx`: now owns `isAiSidebarOpen` state, passes `projectName`/AI-toggle props to `EditorNavbar` and `activeProjectId` to `ProjectSidebar`, and renders `AiSidebar` alongside the canvas only when `activeProject` is set. No Liveblocks, canvas, or AI chat logic was added anywhere, per the spec's explicit scope limits. Verified via `next build` (clean; `/editor/[roomId]` listed as a dynamic route, `next build`'s own TypeScript pass reports no errors) and `eslint` (clean). Browser/session verification used the same throwaway-public-route pattern as Features 01/02/04 (temporary `app/preview-access-denied` and `app/preview-room` routes + matching temporary `proxy.ts` allowlist entries, both reverted immediately after): confirmed via `curl` against the running dev server that `AccessDenied` renders its heading/message/back-link, and that the workspace shell renders the active project's name, the share button, the AI toggle (with correct `aria-label`s for open/closed), the AI sidebar's placeholder copy, and the active room's `bg-accent-dim`/`text-brand` highlight classes in the sidebar list — no visual screenshots were taken since no browser-automation tool was available this session (same gap noted in Feature 07's session notes).

- Feature 09: Share Dialog (`context/feature-specs/09-share-dialog.md`) — `types/collaborator.ts`: `Collaborator` (`id`, `email`, `name: string | null`, `imageUrl: string | null`) — the Clerk-enriched shape returned to the client, distinct from the raw `ProjectCollaborator` Prisma row (which only has `id`/`projectId`/`email`/`createdAt`). `lib/collaborators.ts`: `enrichWithClerkData(collaborators: { id, email }[])` — the "Clerk User Data" enrichment step the spec asked for. Calls `clerkClient()` (the async accessor from `@clerk/nextjs/server`, not the deprecated sync singleton) once with `users.getUserList({ emailAddress: [...] })` for the whole batch rather than one lookup per collaborator, builds a `Map` from every returned user's `emailAddresses[].emailAddress` back to that user, and maps each collaborator to `{ id, email, name: user?.fullName ?? null, imageUrl: user?.imageUrl ?? null }` — falls back to `null`/`null` (rendered as email-only in the UI) when no Clerk user matches, per spec. Returns `[]` immediately for an empty input without calling Clerk. `app/api/projects/[projectId]/collaborators/route.ts`: `GET` requires only `hasProjectAccess` (owner or collaborator, reusing Feature 08's `lib/project-access.ts` helpers) — both roles can list, per spec — `404`s if the project doesn't exist or the caller has no access (same not-found-vs-forbidden ambiguity choice `[roomId]/page.tsx` already made). `POST` requires `project.ownerId === identity.userId` (`403` otherwise), validates the body's `email` against a simple RFC-5322-ish regex (`400` if missing/invalid), lowercases + trims it, then `prisma.projectCollaborator.create`s — a duplicate invite hits the schema's existing `@@unique([projectId, email])` constraint (Feature 05), caught via `error instanceof Prisma.PrismaClientKnownRequestError && error.code === "P2002"` and turned into a `409` rather than a raw 500. `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts`: `DELETE` same ownership check, then verifies the `collaboratorId` row actually belongs to `projectId` before deleting (`404` otherwise) so one owner can't delete another project's collaborator row by guessing an id. `components/editor/share-dialog.tsx`: new dialog built on `editor-dialog.tsx`'s wrappers (matching `project-dialogs.tsx`'s pattern) — fetches `GET .../collaborators` in a `useEffect` scoped to `isOpen`/`projectId` (the fetch itself is wrapped in a nested `async function` invoked from the effect rather than calling `setIsLoading(true)` directly in the effect body, since `eslint-plugin-react-hooks`'s `set-state-in-effect` rule flags the latter as a cascading-render risk); renders an email `Input` + `Invite` button only when `isOwner` is true, an avatar (Clerk `imageUrl` if present, else a `UserRound` icon tile) + name-or-email row per collaborator with a per-row remove `X` button also gated on `isOwner`, and a `Copy link` button that writes `${window.location.origin}/editor/{projectId}` to the clipboard and flips its label to `Copied!` for 2s via `setTimeout`. Collaborators (non-owners) see the same list with no invite input and no remove buttons, per spec. `components/editor/editor-navbar.tsx`'s existing `onShare` prop (added in Feature 08, left unwired) is now wired: `components/editor/editor-shell.tsx` owns `isShareDialogOpen` state, passes `onShare={() => setIsShareDialogOpen(true)}` to the navbar and renders `<ShareDialog isOpen isOwner projectId={activeProject.id} .../>` — both only when `activeProject` is set, matching the existing AI-sidebar-toggle gating pattern. New `isOwner?: boolean` prop on `EditorShell` (defaults `false`), computed in `app/editor/[roomId]/page.tsx` as `project.ownerId === identity.userId` (identity was already being fetched there for `hasProjectAccess`, so no new Clerk/DB call was needed) and passed straight through. No local user table was added — collaborator identity enrichment happens read-through from Clerk on every list request rather than being cached in Postgres, per the spec's explicit "do not add a local user table." Verified via `tsc --noEmit`, `eslint` (both clean after fixing the `set-state-in-effect` violation above), and `next build` (clean; both new collaborator routes listed as dynamic `ƒ` routes alongside the existing project routes). Also verified directly against the real `DATABASE_URL` and Clerk instance with a throwaway `tsx` script (same pattern as Features 06/07): `enrichWithClerkData` correctly resolved the one real Clerk user in this dev instance (`fatihrbl@gmail.com` → name + `imageUrl`) and fell back to `null`/`null` for an email with no matching Clerk user; a full create-project → add-collaborator → list (enriched) → duplicate-invite (rejected by the DB unique constraint, confirming the `409` path's precondition) → remove-collaborator → delete-project round trip against the live database all matched expected behavior. Browser verification used the same throwaway-public-route pattern as Feature 08 (temporary `app/preview-share/page.tsx` + a temporary `/preview-share(.*)` entry in `proxy.ts`'s `isPublicRoute` list, both reverted immediately after): confirmed via `curl` against the running dev server that the workspace chrome renders a `Share project` aria-labeled button, and that both `isOwner=true` and `isOwner=false` variants of the page render `200` without server errors — the dialog's own internal contents (invite form, collaborator rows, copy-link toggle) were not visually screenshotted since no browser-automation tool was available this session (same gap noted in every prior feature's session notes going back to Feature 04).

- Feature 10: Liveblocks Setup (`context/feature-specs/10-liveblocks-setup.md`) — `liveblocks.config.ts`: `Presence` now has `cursor: { x: number; y: number } | null` and `isThinking: boolean`; `UserMeta.info` now has `name`/`avatar`/`color: string`. The other still-unused keys (`Storage`, `RoomEvent`, `ThreadMetadata`, `RoomInfo`) were changed from bare `{}` to `Record<string, never>` — `{}` types the empty-object placeholder as "any non-nullish value" and fails this repo's `@typescript-eslint/no-empty-object-type` lint rule; `Record<string, never>` is the correct "actually empty object" type and was not previously an issue only because this scaffold file (auto-generated by `create-liveblocks-app --init`, per `AGENTS.md`'s block at the top of this file) had never been linted before this feature touched it. `lib/liveblocks.ts` (new): `getCursorColorForUser(userId)` — a simple string hash (`charCodeAt` accumulation) mod a fixed 8-color palette pulled from `ui-context.md`'s existing brand/canvas accent colors (`#00C8D4` cyan brand, `#6457F9` AI indigo, plus 6 of the 8 `NODE_COLORS` text colors), so the same Clerk user ID always maps to the same cursor color, deterministically, with no storage needed. `getLiveblocksClient()` — the "cached Liveblocks node client" the spec asked for, caching a `Liveblocks` (`@liveblocks/node`) instance on `globalThis` (unconditionally, not gated to non-production like `lib/prisma.ts`'s dev-HMR-only gate — see Architecture Decisions for why) but **lazily**, constructed on first call inside a request rather than at module top-level: `@liveblocks/node`'s `Liveblocks` constructor synchronously validates `secret` starts with `sk_` and throws otherwise, which broke `next build`'s page-data-collection step (every route module gets imported during that phase) when `LIVEBLOCKS_SECRET_KEY` is unset — deferring construction until the route handler actually runs fixes this at the root rather than stubbing the env var for build time. `@liveblocks/node` itself had to be `npm install`ed — despite `10-liveblocks-setup.md`'s "All required Liveblocks packages are already installed," only `@liveblocks/client`/`@liveblocks/react`/`@liveblocks/react-flow`/`@liveblocks/react-ui` (client-side packages) were present; the server SDK was missing and is required for `identifyUser`/`getOrCreateRoom`/`updateRoom`. `app/api/liveblocks-auth/route.ts` (new): `POST` — reuses `lib/project-access.ts`'s `getCurrentIdentity()`/`hasProjectAccess()` (the "existing access helper" the spec pointed at) exactly as `app/api/projects/[projectId]/collaborators/route.ts`'s `GET` already does; parses `{ room }` from the request body (the field name Liveblocks' `authEndpoint` POSTs, confirmed via the `liveblocks-best-practices` skill's `auth-endpoint-callback` reference) as the project ID, `401`s if unauthenticated, `400`s if `room` is missing, then `404`→`403`s as a single `403` (not `404`) for both a nonexistent project and an inaccessible one — deliberately different from the collaborators route's `404`, because this spec explicitly says "Return 403 for unauthorized project access," and folding "doesn't exist" into that same `403` avoids leaking project existence to a non-member the same way the `404` choice does elsewhere. Room ID = project ID (no separate room-ID field exists on `Project`, matching Feature 07/08's room-ID-alignment decision). Calls `liveblocks.getOrCreateRoom(projectId, { defaultAccesses: [], usersAccesses: { [userId]: ["room:write"] } })` then `liveblocks.updateRoom(projectId, { usersAccesses: { [userId]: ["room:write"] } })` — rooms are created **private** (`defaultAccesses: []`), with write access granted per-user via `usersAccesses` rather than made room-wide-public, and re-granted via `updateRoom` on every auth call (idempotent, `updateRoom`'s `usersAccesses` merges per-key per the Liveblocks node SDK's `UpdateRoomAccesses` type rather than replacing the whole map) — see Architecture Decisions for why `defaultAccesses: ["room:write"]` was rejected despite being simpler. `currentUser()` (Clerk) supplies `name` (`fullName` → `username` → identity email → `"Anonymous"` fallback chain) and `avatar` (`imageUrl`, `""` if absent); `getCursorColorForUser(userId)` supplies `color`. `liveblocks.identifyUser({ userId, groupIds: [] }, { userInfo: { name, avatar, color } })` — `groupIds: []` is required because this installed SDK version's `Identity` type (`node_modules/@liveblocks/node`) requires `groupIds`, unlike the skill reference's example which shows it as omittable; the route returns `identifyUser`'s `{ status, body }` directly as the response, per the `authenticating-with-id-tokens` reference. No client-side `LiveblocksProvider`/`RoomProvider` wiring was added — `10-liveblocks-setup.md`'s checklist covers only config, the server client, and the auth route, not mounting the provider around the canvas; that's deferred to whichever feature builds the actual React Flow canvas (see Next Up). `LIVEBLOCKS_SECRET_KEY=` (empty) was added to `.env.local` alongside the Clerk keys — **no real value is set**, since this requires a Liveblocks dashboard API key the assistant has no way to generate; the user needs to fill this in from https://liveblocks.io/dashboard/apikeys before the route works at runtime (see Open Questions). Verified via `tsc --noEmit`, `eslint .`, and `next build` (all clean; `/api/liveblocks-auth` listed as a dynamic `ƒ` route) — this is as far as verification could go without a real secret key: unlike every prior feature's live-DB/live-Clerk `tsx` script pattern, `identifyUser`/`getOrCreateRoom`/`updateRoom` cannot be exercised against the real Liveblocks API in this session (see Session Notes).

- Feature 11: Base Canvas (`context/feature-specs/11-base-canvas.md`) — `types/canvas.ts` (new): `NodeColor` (`fill`/`text`), `NODE_COLORS` (the 8 color pairs from `ui-context.md`, in the same order — this is the canonical location `ui-context.md` already pointed at) and `DEFAULT_NODE_COLOR` (`NODE_COLORS[0]`); `NODE_SHAPES` (the 6 shape literals from `ui-context.md`) and derived `NodeShape` union type; `CanvasNodeData` (`label: string`, `color: string`, `shape: NodeShape`, plus a `[key: string]: unknown` index signature — required because `@xyflow/react`'s `Node<NodeData>` generic constrains `NodeData extends Record<string, unknown>`, and a closed interface doesn't structurally satisfy an index-signature constraint even though every declared key is more specific); `CanvasNode = Node<CanvasNodeData, "canvasNode">` and `CanvasEdge = Edge<Record<string, never>, "canvasEdge">` (edges carry no custom data yet, per scope). `components/editor/editor-workspace.tsx`: converted from a static server-renderable placeholder into the client-side room wrapper the spec asked for — now `"use client"`, renders `LiveblocksProvider authEndpoint="/api/liveblocks-auth"` (Feature 10's route) → `RoomProvider id={project.id} initialPresence={{ cursor: null, isThinking: false }}` (room ID = project ID, matching the Feature 07/08 alignment decision; `isThinking` has to be supplied even though the spec only mentions `cursor: null`, since `liveblocks.config.ts`'s `Presence` type — Feature 10 — requires both keys and `RoomProvider`'s `initialPresence` prop is typed against the full `Presence` shape) → `CanvasErrorBoundary` (fallback `CanvasErrorFallback`) → `ClientSideSuspense` (fallback `CanvasLoading`) → `Canvas`. `components/editor/canvas.tsx` (new): calls `useLiveblocksFlow<CanvasNode, CanvasEdge>({ suspense: true, nodes: { initial: [] }, edges: { initial: [] } })` from `@liveblocks/react-flow` and passes `nodes`/`edges`/`onNodesChange`/`onEdgesChange`/`onConnect`/`onDelete` straight into `<ReactFlow>`, with `connectionMode={ConnectionMode.Loose}` (the "loose connection behavior" the spec asked for — any handle can connect to any handle, not just source→target), `fitView`, a `<MiniMap />`, and `<Background variant={BackgroundVariant.Dots} />`; imports `@xyflow/react/dist/style.css` (required for `ReactFlow` to render at all — first component in the codebase to mount it). No custom node/edge `type` renderers, no `Controls`, no persistence logic beyond what `useLiveblocksFlow` already does — all explicitly out of scope per the spec. `components/editor/canvas-error-boundary.tsx` (new): a small hand-written class component (`getDerivedStateFromError`), not the `react-error-boundary` package — that package isn't a project dependency, and adding it for one class component seemed like more than this scope needed; matches the "error fallback for Liveblocks connection issues" checklist item. `components/editor/canvas-error-fallback.tsx` / `components/editor/canvas-loading.tsx` (new): presentational-only, styled to match `components/editor/access-denied.tsx`'s existing icon-tile/heading/message pattern (`WifiOff` icon in a `bg-elevated` tile for the error state, `Loader2` with `animate-spin` for loading) rather than inventing a new visual language. Verified via `tsc --noEmit`, `eslint .`, and `next build` (all clean; route list unchanged from Feature 10, confirming no new routes were needed). Browser verification used the same throwaway-public-route pattern as every prior UI feature (temporary `app/preview-canvas/page.tsx` rendering `EditorWorkspace` directly with a fake project, plus a temporary `/preview-canvas(.*)` entry in `proxy.ts`'s `isPublicRoute` list, both reverted immediately after): `curl`'d the running dev server (an already-running instance on port 3000, reused rather than starting a second one) and confirmed a `200` response containing the `CanvasLoading` fallback's "Loading canvas…" text server-rendered by `ClientSideSuspense`, with no Next.js error overlay markers in the HTML. The dev server's own logs then showed repeated `Error: Invalid value for field 'secret'. Secret keys must start with 'sk_'` from `/api/liveblocks-auth` — this is `lib/liveblocks.ts`'s `getLiveblocksClient()` (Feature 10) failing because `LIVEBLOCKS_SECRET_KEY` is still unset (see Open Questions), and it's actually a useful confirmation: it shows `LiveblocksProvider` mounted, called the auth endpoint with the room ID, and is retrying on failure exactly as expected — the wiring itself is correct, only the missing key blocks a real connection. What was **not** verified: the actual rendered canvas (nodes/edges/MiniMap/dot background) and the `CanvasErrorFallback` UI after a failed connection, since no browser-automation tool was available this session to observe client-side state after hydration (same gap noted in every prior feature's session notes) — `curl` only captures server-rendered HTML, which is always the `ClientSideSuspense` fallback regardless of what happens client-side afterward.

- Feature 12: Shape Panel (`context/feature-specs/12-shape-panel.md`) — `types/canvas.ts`: added `ShapeSize`, `SHAPE_DEFAULT_SIZES: Record<NodeShape, ShapeSize>` (per-shape default dimensions — `rectangle` 160×80 wider-than-tall, `circle` 100×100 square, `diamond` 180×180 — larger than both, "slightly larger so labels have room" per spec — `pill` 160×64, `cylinder` 120×110, `hexagon` 170×100, the latter three sized by sensible judgment since the spec only dictated the first three explicitly), `SHAPE_DRAG_MIME_TYPE` (`"application/x-ghost-shape"`, the custom `dataTransfer` MIME type used for the drag payload so the canvas drop handler can distinguish shape-panel drags from any other drag source), `ShapeDragPayload` (`{ shape, width, height }`), and `getNodeTextColor(fill)` (looks up the matching text color for a given fill from `NODE_COLORS`, falling back to `DEFAULT_NODE_COLOR.text` — needed because `CanvasNodeData` only stores a single `color` field, which was decided here to mean the *fill* hex, with the paired text color always derived rather than stored, so the two can never drift out of sync). `components/editor/shape-panel.tsx` (new): `ShapePanel` — the floating pill-shaped toolbar (`rounded-full`, `bg-surface/90` with `backdrop-blur`, positioned `absolute bottom-6 left-1/2 -translate-x-1/2`), one draggable icon button per `NODE_SHAPES` entry (Lucide `RectangleHorizontal`/`Diamond`/`Circle`/`Pill`/`Cylinder`/`Hexagon`, all confirmed to exist in the installed `lucide-react` version before use), `onDragStart` sets `event.dataTransfer` with the shape's `SHAPE_DEFAULT_SIZES` entry as a JSON-stringified `ShapeDragPayload` under `SHAPE_DRAG_MIME_TYPE`. `components/editor/canvas-node.tsx` (new): `CanvasNodeRenderer`, the custom `"canvasNode"` type renderer the spec asked for — per spec, every shape renders identically this unit: a bordered (`border-surface-border`, `rounded-xl`) box sized from the node's own `width`/`height` (React Flow's `NodeProps` picks these up directly, no measurement pass needed since the drop handler sets them explicitly), filled with `data.color` and text-colored via `getNodeTextColor(data.color)`, label centered. Also renders four `Handle`s (one per side, `Position.Top/Right/Bottom/Left`, target/source alternating to satisfy `ConnectionMode.Loose`) styled `opacity-0 group-hover:opacity-100` — not explicitly asked for by this spec's checklist, but required for the "Connection Handles" convention already documented in `ui-context.md` ("hidden by default, revealed on node hover") to apply to the very first node renderer that exists in the codebase; without them the new nodes would be visible but uneditable/unconnectable. `components/editor/canvas.tsx`: restructured — `Canvas` now just wraps a new inner `CanvasFlow` in `ReactFlowProvider` (needed because `useReactFlow()`'s `screenToFlowPosition` — used for the drop-position conversion — only works inside a `ReactFlowProvider` ancestor, and `<ReactFlow>` only provides that context to its own children, not to sibling elements like the drop-target wrapper `<div>` this feature adds around it). `CanvasFlow` adds: a `nodeTypes={{ canvasNode: CanvasNodeRenderer }}` prop (previously unset — this is what makes existing/new `"canvasNode"`-typed nodes actually render instead of erroring); a wrapping `<div className="relative h-full w-full" onDragOver={...} onDrop={...}>` around both `<ReactFlow>` and the new `<ShapePanel />` (the "canvas wrapper" the spec's `dragover`/`drop` handling attaches to — `onDragOver` calls `preventDefault()` + sets `dropEffect = "copy"`, required for `onDrop` to ever fire per the native HTML5 DnD API); `handleDrop` reads `SHAPE_DRAG_MIME_TYPE` off `dataTransfer` (bails silently if absent/unparseable — e.g. a non-shape drag), calls `screenToFlowPosition({ x: event.clientX, y: event.clientY })` for the canvas-space drop position (used as-is, not centered on the cursor — the spec says only "convert screen position to canvas coordinates," so no centering offset was invented), builds the node ID as `` `${shape}-${Date.now()}-${counter}` `` via a `useRef`-held counter incremented per drop (satisfies "shape name, timestamp, and a counter" without needing the count to survive a remount or be synced — collision risk is already effectively zero from the timestamp alone), and constructs the new node with an empty `label`, `DEFAULT_NODE_COLOR.fill` as `color`, and the dragged `shape`/`width`/`height`. Since `useLiveblocksFlow` (Feature 10/11) exposes no direct `setNodes`/`addNode` — only `onNodesChange` — the node is added via `onNodesChange([{ type: "add", item: newNode }])`, using `@xyflow/system`'s documented `NodeAddChange` shape (confirmed by reading `node_modules/@xyflow/system/dist/esm/types/changes.d.ts` directly, since neither the `liveblocks-best-practices` skill's `multiplayer-react-flow` reference nor the installed `@liveblocks/react-flow` types document an explicit "add a node" example) — this is the same `onNodesChange` callback already wired to `<ReactFlow>`, so Liveblocks syncs the new node exactly like any other change. Verified via `tsc --noEmit`, `eslint` (both clean), and `next build` (clean, route list unchanged — this feature added no routes). Browser/interactive drag-and-drop verification was **not possible** this session — no browser automation tool (`chromium-cli`, Playwright, or equivalent) was available (same recurring gap as every prior UI feature, see Session Notes); used the same throwaway-preview-route pattern as Feature 11 (temporary `app/preview-shape-panel/page.tsx` rendering `EditorWorkspace` with a fake project, plus a temporary `/preview-shape-panel(.*)` entry in `proxy.ts`, both reverted immediately after) to confirm via `curl` that the route still renders `200` with the `CanvasLoading` fallback and no server-side crash from the new `nodeTypes`/wrapper/`ReactFlowProvider` wiring — this does not exercise client-side drag-and-drop, node rendering, or Liveblocks sync, only that nothing broke SSR.

## In Progress

- None yet.

- **Bug fix, `context/current-issues.md` sidebar/canvas audit** (`components/editor/editor-shell.tsx`): the wrapping div around both floating sidebars and the canvas had `overflow-hidden` (`relative flex-1 overflow-hidden`). `position: fixed` descendants (both `ProjectSidebar` and `AiSidebar`) are clipped by an ancestor's `overflow: hidden` in most browsers even when that ancestor isn't their containing block (containing block is the viewport here — no ancestor sets `transform`/`filter`/`perspective`/`will-change`) — a well-known CSS gotcha, and the likely cause of the reported "left sidebar not fully hiding, peeking out" symptom. Removed `overflow-hidden` (now just `relative flex-1`); nothing inside that div relies on it for clipping — sidebars self-hide via `translate-x-full`/`-translate-x-full`, the mobile scrim is `fixed inset-0`, and the canvas is `absolute inset-0` matching the parent's own bounds exactly, so removing it is safe. Audited the rest of `current-issues.md`'s checklist against the current (uncommitted, Feature 11/12) code and the actual SSR-rendered markup (via a temporary public preview route + temporary `proxy.ts` allowlist entry, both reverted after — same throwaway pattern every prior UI feature used, see Feature 04's Architecture Decisions entry): sidebars are already `fixed`/`z-40` above the canvas (`z-auto`) with no push/shrink of canvas width; `EditorWorkspace`'s canvas wrapper is `absolute inset-0 bg-base` with no border/shadow/rounded "card" styling anywhere in `app/editor/**` or `components/editor/canvas.tsx`; the dotted `<Background>` already renders full-bleed since nothing narrows the canvas container. These were already correct in the working tree — no changes needed beyond the `overflow-hidden` removal. Drag-and-drop (shape panel → canvas) was re-audited against the installed `@liveblocks/react-flow`/`@xyflow/system` type definitions (`onNodesChange`'s type is `OnNodesChange<N>`, identical to vanilla `@xyflow/react`'s, and `NodeProps.width`/`.height` are `Pick`ed directly from the node object's own `width`/`height` fields — confirming the `{ type: "add", item: newNode }` change and the explicit `width`/`height` set in `handleDrop` both match the documented contract) but could not be interactively verified — same `chromium-cli`/Playwright/Puppeteer gap every prior session hit (see Feature 12's Session Notes), and this session additionally had no network access via `curl` (blocked by the sandbox's permission classifier; only `node -e "fetch(...)"` worked, which is enough for SSR markup but not pointer/DnD simulation). `tsc --noEmit` and `eslint .` both clean.

## Next Up

- Do a real click-through of `/editor/[roomId]` in a signed-in browser session (two sessions, ideally) to confirm: the canvas connects and renders, dragging each of the 6 shapes from the bottom panel creates a node with the right size/shape/empty label at the drop position, the node is visible/bordered with centered (empty) label text, connection handles appear on hover and can be dragged to create an edge, and all of this syncs to a second connected session. This is now the top verification priority across Features 11 and 12 combined, since `LIVEBLOCKS_SECRET_KEY` is set (see Open Questions) but no interactive browser session has ever successfully exercised any of it.
- No feature-spec yet defines node selection/editing (setting a non-empty label), shape-specific visuals (diamond/hexagon/cylinder as inline SVG per `ui-context.md`, currently all shapes render as the same bordered rectangle per this feature's explicit scope), canvas persistence (snapshots to Vercel Blob per `architecture-context.md`), or AI-driven generation into the canvas. Whichever feature adds node editing/selection should be next in the canvas track.

## Open Questions

- ~~`LIVEBLOCKS_SECRET_KEY` in `.env.local` is empty...~~ Resolved: a real `sk_dev_...` key is now set in `.env.local`. Still unverified live (see Next Up) — no browser session has exercised a real Liveblocks connection yet, so it's unconfirmed whether the key is valid/active on the Liveblocks dashboard side.
- `app/layout.tsx` still has the default create-next-app `<title>`/`<meta description>` — not updated, since branding/metadata wasn't in scope for this change.
- ~~`types/project.ts`'s mock `Project` type has a `slug` field...~~ Resolved in Feature 07: dropped `slug` (and `role`) from `Project` entirely — see that entry in Completed.
- `06-project-apis.md`'s `GET /api/projects` route itself is still owner-only, unchanged by Feature 07 (that spec's checklist is about the route, not `/editor`'s data fetching, which now goes through `lib/projects.ts` directly rather than through this route). Revisit if a future feature needs collaborators to see shared projects through the REST endpoint itself, e.g. for a non-browser client.
- ~~`app/editor/[projectId]/page.tsx` (added in Feature 07, not requested by any feature-spec) is a bare-bones placeholder...~~ Resolved in Feature 08: the route is now `app/editor/[roomId]/page.tsx` per `08-editor-workspace-shell.md`'s explicit naming, with real access-denied handling (`AccessDenied` instead of `notFound()`) and the full workspace chrome (navbar actions, active-room highlighting, AI sidebar placeholder). The canvas itself (`EditorWorkspace`'s "Canvas coming soon.") is still a placeholder — no feature-spec defines Liveblocks/React Flow/presence yet; whichever feature builds the real canvas should replace `components/editor/editor-workspace.tsx`'s contents, not the route.

## Architecture Decisions

- This project's `shadcn` CLI (v4.19.0) generates components on `@base-ui/react` primitives with the `base-nova` style, not the classic Radix-based `new-york`/`default` style from most shadcn documentation and training data. Generated files use `data-slot`, `data-open`/`data-closed`, and a `render` prop (not `asChild`) for composition. Future `shadcn add` calls will follow this same convention automatically — do not hand-write Radix-based patterns.
- shadcn's semantic color tokens (`background`, `foreground`, `card`, `popover`, `primary`, `secondary`, `muted`, `destructive`, `border`, `input`, `ring`) are defined in `app/globals.css` as aliases onto the brand tokens in `context/ui-context.md` (`--bg-base`, `--text-primary`, `--accent-primary`, etc.), so unmodified `components/ui/*` files render in the correct dark theme. App-level code should keep using the brand utility names (`bg-base`, `text-copy-primary`, `border-surface-border`, `text-brand`, `bg-accent-dim`, `bg-ai`, `text-ai-text`, `bg-state-error`, etc.) rather than the shadcn semantic names, to stay aligned with `ui-context.md`.
- Theme is forced dark via a `dark` class on `<html>` in `app/layout.tsx` (no toggle, no light variant) since the product has no light mode.
- `EditorNavbar` and `ProjectSidebar` (`context/feature-specs/02-editor-chrome.md`) are stateless/controlled: they take `isSidebarOpen`/`isOpen` plus callback props rather than owning state themselves, since the spec describes reusable chrome shells, not a page. `ProjectSidebar` is `fixed`, positioned `top-14` (below the navbar) so it floats as an overlay without pushing canvas content, per the "floating sidebar overlay" layout pattern in `ui-context.md`. `components/editor/editor-shell.tsx` is the component that owns the toggle state and composes the two — kept separate from `EditorNavbar`/`ProjectSidebar` so those stay easy to test and reuse independently of how their state is managed.
- `components/editor/editor-dialog.tsx` implements the "Dialog Pattern" from the spec as styled wrappers (`EditorDialogContent`, `EditorDialogFooter`) around the unmodified `components/ui/dialog.tsx` primitives, rather than a new dialog component — this keeps `components/ui/*` untouched per the protected-foundation-components rule while giving future features brand-token styling (`rounded-3xl`, `bg-surface`) to compose actual dialogs from.
- Auth uses `proxy.ts`, not `middleware.ts` — this Next.js version (16) renamed the convention (same `NextMiddleware` contract, file/export name only), per `node_modules/next/dist/docs/01-app/01-getting-started/16-proxy.md`. `@clerk/nextjs` (v7.8.2) already ships proxy-aware exports for this.
- Route protection is centralized in `proxy.ts` via `clerkMiddleware` + `createRouteMatcher` (allowlist the sign-in/sign-up routes, `auth.protect()` everything else) rather than per-page `auth()` checks, per the explicit `03-auth.md` spec ("Protect everything else by default", checklist requires `proxy.ts` to exist and cover all routes). Note: the installed `@clerk/nextjs` deprecates `createRouteMatcher` in favor of resource-based (per-page) auth checks — it still works and is not removed in this version, so it was kept to match the spec; revisit if it's removed in a future `@clerk/nextjs` upgrade (migration guide: https://clerk.com/docs/guides/development/upgrading/upgrade-guides/migrate-from-create-route-matcher).
- Clerk appearance is configured once, globally, in `app/layout.tsx`'s `ClerkProvider` (`theme: dark` from `@clerk/ui/themes` + `variables` mapped to the app's CSS custom properties) rather than per-component — every Clerk component (`SignIn`, `SignUp`, `UserButton`, etc.) inherits it automatically, so no separate appearance config was added for `UserButton` in the navbar.
- `components/editor/editor-shell.tsx` (`context/feature-specs/04-project-dialogs.md`) dropped the generic `children: ReactNode` slot it had from Feature 02 and now owns `useProjectDialogs()` directly, rendering `EditorHome` in that slot itself — there is currently only one thing `/editor` ever shows there (the empty state), so a render-prop/context indirection to keep the slot generic would be speculative; revisit if a future feature (e.g. an opened-project canvas view) needs the shell to swap what renders in that position.
- Project CRUD in Feature 04 is fully client-side/in-memory (`hooks/use-project-dialogs.ts` mutates a `useState` array seeded with mock data) per the spec's "mock project data only, no API calls or persistence" — this will need to be swapped for real `app/api` routes + Prisma once project persistence is in scope; the hook's public interface (`myProjects`/`sharedProjects`/dialog openers/`isLoading`) was kept deliberately thin so that swap shouldn't need to change `project-dialogs.tsx` or `project-sidebar.tsx`.
- Verifying `/editor` UI work is awkward because the whole route is behind Clerk auth (`proxy.ts`) and no test session is set up in this environment. Feature 04 was verified by temporarily adding a public `app/preview-editor` route (rendering the same `EditorShell`) and a matching temporary entry in `proxy.ts`'s `isPublicRoute` list, both removed immediately after the Playwright screenshots were taken — same throwaway-preview-route pattern Feature 01/02 used, just also touching `proxy.ts` since Feature 03 added auth. If auth verification keeps being a bottleneck, consider a documented seeded test user instead of re-deriving this each time.
- **Prisma CLI pinned to `^7.10.0`, not the `prisma` package's `latest` npm tag.** `latest` currently resolves to `8.0.0-rc.10`, a pre-release that replaces the classic `PrismaClient`/`generator client { output = ... }` model entirely with a new "contract" system (`prisma orm init`, `prisma contract emit`, a `postgres()` runtime client reading `contract.json`/`contract.d.ts` instead of a generated `PrismaClient`) — no equivalent of a plain generated-client output path exists in that CLI. `@prisma/client`/`@prisma/adapter-pg` are pinned to `^7.10.0` (the last classic-model release, `prev` npm tag on `@prisma/client`, `prev`/pre-8.0.0-rc on `prisma`), so `prisma` must stay on the matching `7.10.0` classic CLI until this project deliberately migrates to the new contract model — do not bump `prisma` to `^8` / run `prisma orm init` without re-reading `npx prisma --help` first, since the command surface is unrelated to classic Prisma docs/training data (same caveat `AGENTS.md` gives for Next.js).
- **Bug fix, Feature 01 design system** (found while building Feature 04, fixed in `app/globals.css`): the brand token `--color-base: var(--bg-base)` in the `@theme inline` block was registered under Tailwind's `--color-*` namespace, which auto-generates `text-*`/`border-*`/`ring-*`/etc. utilities for every color key — not just `bg-*`. Because the key was literally named `base`, this collided with Tailwind's own built-in `text-base` font-size scale keyword, and the color utility won the cascade: any element sized with the plain `text-base` class (no other explicit color class) silently got `color: var(--bg-base)` — near-black text on a near-black background. This was invisible in every dialog title (`DialogTitle` in `components/ui/dialog.tsx` uses `text-base` for sizing) and in every `Input`/`Textarea` value (both use `text-base`), reported by the user as "typed text doesn't show" in the Create Project dialog, and also affected `CardTitle`. Root-caused (not patched per-component, since `components/ui/*` is protected foundation code and the defect wasn't in those files) by removing `--color-base` from the theme block and re-declaring `bg-base` as a plain `@utility bg-base { background-color: var(--bg-base); }` outside the color namespace — Tailwind no longer thinks `base` is a color name, so `text-base` reverts to its built-in font-size meaning, while `bg-base` keeps working exactly as before (verified: computed `background-color` on `.bg-base` unchanged, computed `color` on `DialogTitle`/`Input` now resolves to `--text-primary` as intended). No other brand token name collides with a Tailwind scale keyword (checked: surface, elevated, subtle, copy-primary, copy-secondary, copy-muted, copy-faint, brand, accent-dim, ai, ai-text, state-error, state-success, state-warning — none match Tailwind's built-in size/color keyword lists). Take-away for future tokens: avoid naming a brand color the same as a Tailwind core scale keyword (`xs`/`sm`/`base`/`lg`/`xl`/`2xl`.../`DEFAULT`), since `--color-*` collisions like this fail silently (no build error, no lint error — only visibly wrong computed color) and are easy to miss in a quick visual check at normal screenshot scale (had to zoom in to actually notice the low-contrast text).

- **Feature 07 resolves the Feature 04 "revisit" notes above**: `editor-shell.tsx` now takes `activeProject?: Project | null` as a prop (server-fetched by whichever page renders it) instead of always showing `EditorHome` — this is the "opened-project canvas view" case anticipated in Feature 04's note. `hooks/use-project-dialogs.ts` (mock, in-memory) was deleted outright and replaced by `hooks/use-project-actions.ts`, which hits the real API and has no local project-array state at all — `myProjects`/`sharedProjects` are no longer part of the hook's return value, since that data now comes from the server component as props, not from client state.
- **Room ID / project ID alignment (`07-wire-editor-home.md`)**: the spec asks for a client-generated "room ID" (slugified name + short suffix) shown as a preview in the create dialog, and says "the project ID and Liveblocks room ID should stay aligned." Two readings were possible: (a) send the client-generated room ID to `POST /api/projects` as a custom `id`, or (b) treat the preview as cosmetic only and let the server's real `cuid` id be the room id. Went with (b): `POST /api/projects` was **not** modified to accept a client-supplied `id` — `06-project-apis.md` was explicit ("use the schema's existing ID strategy, do not add sequential IDs"), and since the `Project` schema (Feature 05) has no separate `roomId` field, defining "room ID" as always-equal-to `project.id` makes the two values structurally unable to diverge, without touching the API surface 06 already shipped and verified. The create dialog's room ID preview (`hooks/use-project-actions.ts`) is therefore an approximation shown before the real id is known, not a value the backend ever sees. Revisit this if a future Liveblocks-wiring feature needs the room id to be human-readable/predictable before creation (that would require the option-(a) approach instead).
- **Shared projects are looked up by email, not Clerk user ID**, because `ProjectCollaborator` (Feature 05) only stores `email` — there's no Clerk-user-ID column to join on. `lib/auth.ts`'s `getCurrentUserEmail()` calls Clerk's `currentUser()` (a network round-trip to Clerk's backend API, unlike `auth()` which reads the local session JWT) to get `primaryEmailAddress`. This means every render of `/editor` and `/editor/[projectId]` makes one extra Clerk API call beyond what `auth()` alone would need. Acceptable for now given the project's scale; revisit (e.g. cache the email in a JWT claim via a Clerk session token customization) if this becomes a latency concern.

- **Liveblocks rooms are private (`defaultAccesses: []`), not room-wide-public (`defaultAccesses: ["room:write"]`), in `app/api/liveblocks-auth/route.ts`.** ID-token auth (per the `liveblocks-best-practices` skill's `authenticating-with-id-tokens` reference) returns a general identity token for the user, not one cryptographically scoped to only the specific room requested in that POST body — Liveblocks' own servers separately check the target room's `defaultAccesses`/`groupsAccesses`/`usersAccesses` against the identified user whenever a `RoomProvider` actually connects. If `defaultAccesses` were `["room:write"]`, *any* authenticated app user's token (obtained legitimately for a project they do own access to) could be reused by a client to connect directly to a *different*, private project's room, bypassing this route's per-project `hasProjectAccess` check entirely — since that check only runs when this HTTP endpoint is called, not when the Liveblocks realtime connection itself is established. Keeping rooms private and granting `usersAccesses: { [userId]: ["room:write"] }` per authorized user (re-granted via `updateRoom` on every auth call, so newly-added collaborators get access on their next connect without a separate sync step) makes Liveblocks itself enforce the same membership boundary this route already checks, matching `architecture-context.md`'s invariant "Auth and ownership are enforced at every mutation boundary."
- **`lib/liveblocks.ts`'s `getLiveblocksClient()` caches on `globalThis` unconditionally**, unlike `lib/prisma.ts`'s `NODE_ENV !== "production"` gate. The gate in `lib/prisma.ts` exists to survive dev-mode HMR module re-evaluation (production doesn't need it since a module only evaluates once per cold start regardless). Because `getLiveblocksClient()` is a lazy function (client built on first *call*, not at module top-level — see Feature 10's Completed entry for why), the eager-construction problem `lib/prisma.ts`'s gate exists for doesn't apply the same way; caching on `globalThis` in all environments is simpler and still correct, since `globalThis` persists for the lifetime of any given process/serverless instance either way.

## Session Notes

- Add context needed to resume work in the next session.
- Feature 07: same browser-automation gap as Features 04/06 (see below) — no tool was available to actually click through create → navigate → rename → delete in a signed-in browser session. What *was* verified: `tsc`/`eslint`/`next build` all clean with both new routes listed; `lib/projects.ts`'s `getOwnedProjects`/`getSharedProjects` directly against the real database via a throwaway script (same pattern Feature 06 used, see that entry's Completed note and this feature's Completed note for exact assertions); `app/editor/[projectId]/page.tsx`'s `notFound()`-for-both-missing-and-no-access logic and `hooks/use-project-actions.ts`'s `router.push`/`router.refresh` branching were verified by code review only, not live. If a seeded test session becomes available, prioritize exercising this feature's full create→rename→delete loop first, since it's the first feature where wrong behavior would touch real data (previous features either didn't hit the API or didn't wire UI to it).
- Live `curl` testing of the `401` path for Feature 06's new routes was inconclusive in this dev environment: `proxy.ts`'s `auth.protect()` (Feature 03) intercepts every unauthenticated request — page or API — before it reaches a route handler, and in this local Clerk dev instance it responds with `x-clerk-auth-reason: dev-browser-missing` (a `307` redirect to `/sign-in`, or a `404` rewrite depending on headers) instead of ever reaching our code, because `curl` can't complete Clerk's dev-browser JWT handshake (a one-time client-side redirect Clerk's JS performs on first page load in a real browser). Confirmed this is pre-existing and unrelated to this feature by hitting `/editor` (an existing protected page) unauthenticated and seeing the identical `dev-browser-missing` behavior. No browser automation tool was available this session (`claude-in-chrome` was installed but the user chose not to connect it) to complete the handshake and verify the real 401 JSON response a signed-out `fetch()` call would get. The route handlers themselves use the exact `isAuthenticated`/`401` JSON pattern the `clerk-nextjs-patterns` skill documents as correct for this SDK version, so this is believed correct but not end-to-end verified through the live proxy. If this keeps being a bottleneck for API-route auth verification, consider a documented seeded test session (per Feature 04's session notes making the same ask for UI routes).
- Feature 10: `LIVEBLOCKS_SECRET_KEY` is unset in this environment (no Liveblocks dashboard account/key was available this session), so `identifyUser`/`getOrCreateRoom`/`updateRoom` in `app/api/liveblocks-auth/route.ts` were verified only by code review, `tsc`/`eslint`/`next build`, and cross-checking the request/response shapes against the `liveblocks-best-practices` skill's reference docs and the installed `@liveblocks/node` type definitions directly (`node_modules/@liveblocks/node/dist/index.d.ts` — this is where the `groupIds: []` requirement and `UpdateRoomAccesses`/merge-semantics assumption came from, since the skill's own example code showed `groupIds` as optional but this installed version's `Identity` type requires it). Once a real key is set (see Open Questions), prioritize a live round-trip: call the route directly with a valid project ID + session, confirm the returned token's `status`/`body` shape, confirm the room actually gets created in the Liveblocks dashboard with the expected private `defaultAccesses`, and confirm a second collaborator's auth call adds their `usersAccesses` entry without clobbering the first user's.
- Feature 09: same browser-automation gap as every prior feature — `ShareDialog`'s invite/remove/copy-link interactions were not click-tested in a real browser, only verified by code review, `tsc`/`eslint`/`next build`, and a live-DB/live-Clerk `tsx` script exercising the underlying logic (`lib/collaborators.ts`'s enrichment plus the full invite/list/duplicate-reject/remove/delete round trip — see this feature's Completed entry for exact assertions). This dev Clerk instance currently has exactly one real user (`fatihrbl@gmail.com`), which was reused as both a stand-in collaborator's email and the enrichment-hit case in that script; a second real user would be needed to verify two *different* collaborators' avatars/names render distinctly in the same list. If a seeded multi-user test session becomes available, prioritize this feature's full open-dialog → invite → see-avatar → remove loop, plus confirming the copy-link `Copied!` timeout visually.
- Feature 12: same browser-automation gap as every prior UI feature — `chromium-cli` is not installed, and `npx playwright`/`node_modules/@puppeteer` resolve to nothing usable in this environment, so drag-and-drop (which needs real pointer/DnD event simulation, not just DOM inspection) could not be exercised at all this session. Verified instead by: `tsc`/`eslint`/`next build` all clean; reading `@xyflow/system`'s and `@liveblocks/react-flow`'s installed `.d.ts` files directly to confirm the `NodeAddChange` shape and that `useLiveblocksFlow` has no separate `addNode` API; and the `curl`-based SSR smoke test described in this feature's Completed entry. If a browser-automation tool becomes available, this feature and Feature 11 should be verified together in one pass (see Next Up) since neither has ever had a real interactive session.
- Feature 11: blocked on the same missing `LIVEBLOCKS_SECRET_KEY` Feature 10 flagged — confirmed live (not just by code review this time) that `/api/liveblocks-auth` throws `Invalid value for field 'secret'` on every call, via the dev server's own logs while curl-testing the preview route. This means `RoomProvider` has never successfully connected to a real Liveblocks room in this environment yet, so `useLiveblocksFlow`'s empty-nodes/empty-edges initial state, `onConnect`/`onNodesChange` sync behavior, and the `MiniMap`/dot-`Background` rendering are all unverified beyond `tsc`/`eslint`/`next build` and reading the `@liveblocks/react-flow` type definitions directly (`node_modules/@liveblocks/react-flow/dist/index.d.ts`). Once a real key is set, prioritize: opening `/editor/[roomId]` in two browser sessions signed in as different users, confirming both see the same (empty) canvas, dragging to create a node manually via React Flow's default interactions (there's no "add node" UI yet — see Next Up) to confirm it syncs between sessions, and deliberately breaking the connection (bad room ID or revoked access) to confirm `CanvasErrorFallback` renders instead of a blank screen or unhandled exception.
